{
  "name": "booksys-api",
  "version": "0.1.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "clean": "rimraf dist",
    "start": "NODE_ENV=development node -r ts-node/register ./src/server.ts",
    "start:debug": "NODE_ENV=development node --inspect-brk -r ts-node/register ./src/server.ts",
    "copy-env": "cp -r ./src/config/environments ./dist/config",
    "compile": "yarn run clean && tsc -p ./tsconfig.prod.json && yarn run copy-env",
    "typeorm": "ts-node ./node_modules/.bin/typeorm -f ./src/config/ormconfig.ts",
    "migrate": "node -r ts-node/register ./migrations/execute.ts"
  },
  "keywords": [],
  "author": "Ricardo Luz",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^16.10.2",
    "rimraf": "^3.0.2",
    "ts-node": "^10.2.1",
    "typescript": "^4.4.3"
  },
  "dependencies": {
    "@nestjs/common": "^8.0.9",
    "@nestjs/core": "^8.0.9",
    "@nestjs/platform-express": "^8.0.9",
    "class-transformer": "^0.4.0",
    "class-validator": "^0.13.1",
    "date-fns": "^2.25.0",
    "dotenv": "^10.0.0",
    "mysql": "^2.18.1",
    "reflect-metadata": "^0.1.12",
    "rxjs": "^7.1.0",
    "typeorm": "^0.2.38"
  }
}
